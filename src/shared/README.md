src/
├── api/          # Конфигурация API (инстансы axios, базовые запросы)
├── config/       # Конфиг приложения (env, роутинг, i18n)
├── hooks/        # Утилиты и хелперы (хуки)
├── ui/           # UIKit (глупые компоненты: Button, Input, Modal)
└── model/        # Общие типы (TypeScript) и глобальные схемы данных